const Config = {
  minZoom: .1,
  maxZoom: 20,
  grid: {
    linesColor: 'aaa',
    fillColor: 'blue',
    lineWidth: .1,
    fillOpacity: .15,
    borderWidth: 4
  },
  atoms: {
    movColor: 'red',
    fixColor: 'blue',
    splColor: 'green',
    ifColor: 'grey',
    jobColor: 'yellow'
  }
}

export default Config;