const Config = {
  minZoom: .1,
  maxZoom: 20,
  zoomDivider: 1.3,
  grid: {
    linesColor: 'aaa',
    fillColor: 'blue',
    lineWidth: .1,
    fillOpacity: .15,
    borderWidth: 4,
    stepSize: 40,
    query: 'canvas',
    rows: 10,
    cols: 10
  },
  atoms: {
    movColor: 'red',
    fixColor: 'blue',
    splColor: 'green',
    ifColor: 'grey',
    jobColor: 'yellow'
  }
}

export default Config;